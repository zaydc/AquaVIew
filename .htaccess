# Activation du module de réécriture d'URL
RewriteEngine On

# Définition du répertoire de base pour les réécritures
RewriteBase /

# Rediriger toutes les requêtes vers le frontController
# Sauf si c'est un fichier ou un dossier qui existe réellement
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Réécriture vers le frontController dans le dossier web
RewriteRule ^(.*)$ web/frontController.php [QSA,L]

# Définir le fichier par défaut
DirectoryIndex web/frontController.php

# Options de sécurité
Options -Indexes
ServerSignature Off
